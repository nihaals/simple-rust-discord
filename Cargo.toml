[package]
name = "simple-rust-discord"
version = "0.1.5"
edition = "2024"

[dependencies]
serenity = { version = "0.12", default-features = false, features = ["client", "gateway", "model", "rustls_backend"] }
tokio = { version = "1", default-features = false, features = ["rt-multi-thread"] }
